import { DomainImplementation } from 'domains/common/implementations/domains.implementation';

export const domain: DomainImplementation = {
    protocol: 'https',
    url: 'localhost',
    port: 3000
};
