import { EnvironmentImplementation } from 'environments/common/implementations/environment.implementation';

export const environment: EnvironmentImplementation = {
  production: true,
  enableRouterTracing: false
};
